type User {
  id: ID! @id
  name: String!
  dog: Dog!
  phoneNumber: String!
  bookmarks: [Bookmark!]!
}

type Dog {
  id: ID! @id
  name: String!
  owner: User!
}

type Bookmark {
  id: ID! @id
  label: String!
  page: String!
  numberOfVisits: Int!
  user: User!
}
