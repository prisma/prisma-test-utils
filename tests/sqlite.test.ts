import Photon from './dbs/sqlite/@generated/photon'

describe('sql:', () => {
  test.todo('generates test-utils correctly')
  test('seeds data', async () => {
    const client = new Photon({})
  })
})
